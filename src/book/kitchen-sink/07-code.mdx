---
section: Kitchen Sink
chapter: Markdown
title: Code
description: Code blocks in Markdown
keywords:
  - markdown
  - code blocks
slug: /kitchen-sink/code
hidden: true
---

## Code blocks

```jsx
import React from 'react'
import Highlight, { defaultProps } from 'prism-react-renderer'

export default ({ children, className }) => {
  const language = className.replace(/language-/, '')

  return (
    <Highlight {...defaultProps} code={children.trim()} language={language}>
      {({ className, style, tokens, getLineProps, getTokenProps }) => (
        <code className={`${className} w-full p-4 rounded`} style={style}>
          {tokens.map((line, i) => (
            <div key={i} {...getLineProps({ line, key: i })}>
              {line.map((token, key) => (
                <span key={key} {...getTokenProps({ token, key })} />
              ))}
            </div>
          ))}
        </code>
      )}
    </Highlight>
  )
}
```

### Code in a grid

<Grid columns='1fr 1fr' caption="Hello world in languages used in NUbook" seamless>

```cpp
// C++
int main(void) {
  std::cout << "Hello world!" << std::endl;
  return 0;
}
```

```py
# Python
print('Hello world!')
```

```js
// JavaScript
console.log('Hello world!')
```

```bash
# Bash
echo "hello world"
```

```yaml
# YAML
- msg: "hello world"
```

```md
<!-- Markdown -->
# Hello world
```

```docker
# Docker
RUN echo "Hello world"
```

```cmake
# CMake
message(STATUS "Hello world")
```
</Grid>
